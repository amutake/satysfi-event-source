@require: standalone
@import: ../callbacks

let c1 = Callbacks.make () in
let () = Callbacks.register display-message c1 in
let () = Callbacks.run `foo` c1 in
let () = Callbacks.register display-message c1 in
let () = Callbacks.run `bar` c1 in

let t2 = Callbacks.make () in
let () = Callbacks.run `baz` c2 in % no output

standalone '<>
